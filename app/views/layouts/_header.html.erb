<header>
  <div class="navbar navbar-nav navbar-fixed-top header navbar-default navbar-fixed-top">
  <div class="container">
      
 
        <div class=" navbar-center">
        <%= link_to image_tag("logo-cor.png"), root_path, id: "logo" %>
        </div>





      <nav>
        <ul class="nav navbar-nav navbar-right">

        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_user.first_name %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Dashboard", dashboard_path %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Logout", logout_path, method: "delete" %>
              </li>
              

            </ul>
          </li>
          <li><%= link_to "Log Out", logout_path, method: "delete" %></li>
        <% else %>
          <li><%= link_to "Log In", login_path %></li>
          <li><%= link_to "Sign Up", signup_path %></li>
        <% end %>
          <li class=""><%= link_to "Rack It", rackit_path %></li>
        </ul>
      </nav>
  </div>
  </div>

<% if params[:controller] == 'users' && %w(dashboard account).include?(params[:action]) ||
                              'items' && %w(index my_reservations requests).include?(params[:action]) %>
  <% if params[:commit] != "Search" %>
  <%= render 'layouts/secondnavbar' %>
  <% end %>
<% end %>
<% if params[:controller] == 'static_pages' && %w(home ).include?(params[:action]) %>
 
<% end %>



  
</header>

<script src="http://maps.googleapis.com/maps/api/js?sensor=false&amp;libraries=places"></script>












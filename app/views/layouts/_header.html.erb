<header>
  <div class="navbar navbar-nav navbar-fixed-top navbar-inverse header">
  <div class="container">
      
  	  <% if current_page?(root_url) %>      
        <%= link_to image_tag("logosmall.png"), root_path, id: "logo" %>
      <% else %>
      <div class="navbar-center">
      	<%= link_to image_tag("logo-no-text-small.png"), root_path, id: "logo-small" %>
      </div>
      <% end %>

      <nav>
        <ul class="nav navbar-nav navbar-right">

        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Logout", logout_path, method: "delete" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log In", login_path %></li>
          <li><%= link_to "Sign Up", rack_path %></li>
          <li><%= link_to "Rack It", account_path %></li>
        <% end %>

        </ul>
      </nav>
  </div>
  </div>

<% if params[:controller] == 'users' && %w(dashboard rack account).include?(params[:action]) %>
  <%= render 'layouts/secondnavbar' %>
<% end %>


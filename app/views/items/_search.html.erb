<div class="jumbotron">

<div class="row">
<% @items.each do |item| %>
  <%= link_to item_path(item) do %>

<% item.photos.first(1).each do |photo| %>
  
    
    <div class="col-sm-6 col-md-4">

      <div class="thumbnail" >
        
        <%= image_tag photo.image_url(:thumb) if photo.image? %>
        <div class="center">
        <span class="badge">$<%= item.lending_price %> per day</span>
        </div>
        <hr>     
        <div class=" center">
          <h2><%= item.item_name %></h2>
          <h2><%= item.location.city %> <%= item.location.state %> </h2>
        </div>



      </div>
    </div>
  <% end %>
  
 <% end %>


<% end %>
</div>
</div>